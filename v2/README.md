# Retired V2 preview

Production moved to the repository root on 2026-09-22. Edit root pages, `assets/`, `admin/` and `tools/`. HTML entry points here redirect to their root equivalents. Old assets remain for backward compatibility with shared media URLs. Historical checkpoint files describe the pre-promotion state; root README.md is current.
